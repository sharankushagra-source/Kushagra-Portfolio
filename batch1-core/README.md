# Kushagra Sharan — Portfolio

Static HTML/CSS/JS site wrapped in a minimal Vite project for Vercel deployment.

## Local dev
```
npm install
npm run dev
```

## Deploy
Push to GitHub, import into Vercel. Build command: `npm run build`. Output: `dist`.
